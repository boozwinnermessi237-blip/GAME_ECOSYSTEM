// main.cpp 
#include "Core/GameEngine.hpp"
#include <iostream>
#include <cstdlib>
#include <ctime>

int main(int argc, char* argv[])
{
    // -------------------------------------------------------------
    //  Initialisation de l'aléatoire
    // -------------------------------------------------------------
    std::srand(static_cast<unsigned int>(std::time(nullptr)));

    // -------------------------------------------------------------
    //  Bannière de présentation
    // -------------------------------------------------------------
    std::cout << R"(
    ╔═══════════════════════════════════════════════════╗
    ║                                                   ║
    ║   🌍 SIMULATEUR D'ÉCOSYSTÈME INTELLIGENT 🌿        ║
    ║                                                   ║
    ║          Projet de Programmation Orientée Objet   ║
    ║                     en C++ / SDL3                 ║
    ║                                                   ║
    ╚═══════════════════════════════════════════════════╝
    )" << std::endl;

    // -------------------------------------------------------------
    // 🏗 Création du moteur de jeu
    // -------------------------------------------------------------
    Ecosystem::Core::GameEngine engine(
        "Simulateur d'Écosystème Intelligent",
        1200.0f,
        800.0f
    );

    // -------------------------------------------------------------
    // ⚙ Initialisation du moteur
    // -------------------------------------------------------------
    if (!engine.Initialize())
    {
        std::cerr << "\n❌ ERREUR CRITIQUE : Échec de l'initialisation du moteur SDL3.\n";
        std::cerr << "➡ Vérifiez que la bibliothèque SDL3 est installée correctement.\n";
        return EXIT_FAILURE;
    }

    // -------------------------------------------------------------
    //  Lancement de la boucle principale
    // -------------------------------------------------------------
    engine.Run();

    // -------------------------------------------------------------
    // X Fermeture propre
    // -------------------------------------------------------------
    engine.Shutdown();

    // -------------------------------------------------------------
    //  Message de fin
    // -------------------------------------------------------------
    std::cout << "\n╔═══════════════════════════════════════════════════╗\n";
    std::cout <<   "║       Merci d'avoir testé le simulateur !       ║\n";
    std::cout <<   "║             Programme terminé avec succès.         ║\n";
    std::cout <<   "╚═══════════════════════════════════════════════════╝\n\n";

    return EXIT_SUCCESS;
}
